// import { EntityIFace } from '../../Core/interface/EntityIFace';
// import { ResponseIFace } from '../../Core/interface/ResponseIFace';
export interface Todo{
  // export interface Todo extends EntityIFace{
  id: string,
  userId: string,
  title: string,
  completed: string
}

// export interface TodoList extends ResponseIFace<Todo> {}
